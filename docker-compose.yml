services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file: .env
  frontend:
    build:
      context: ./frontend
      args:
        - NEXT_PUBLIC_API_URL=http://your-server-ip:8000
    ports:
      - "3000:3000"
    depends_on:
      - backend